# PostgreSQL Database Configuration for Production
spring.datasource.url=jdbc:postgresql://localhost:5432/mess_db 
spring.datasource.username=postgres
spring.datasource.password=Postgres@1234
#Password for PostgreSQL
spring.datasource.driverClassName=org.postgresql.Driver

# JPA/Hibernate Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update 
# Or 'none'. Use migrations for schema changes in prod.
spring.jpa.show-sql=true 
# Usually false in production for performance/security

app.cookie.secure=true

# ... (Stripe API keys) ...
stripe.price-id.monthly-subscription=price_1RVUMKI6dSDHz36k8aRbk0vp

# Frontend URLs for Stripe Checkout redirects
app.frontend.url.base=http://localhost:3001 
# Change if your React dev server is different
app.frontend.url.subscription.success=/payment/success?session_id={CHECKOUT_SESSION_ID}
app.frontend.url.subscription.cancel=/payment/cancel
